<% @page_description = t(:index_description) -%>
<% @page_title = controller.controller_name.capitalize + '| ' + 'Edit' -%>

<% @back_path = admin_passports_path %>
<% @show_path = admin_passport_path(@passport) %>

<div id="staticContent" class="clearfix">
  <div class="onecolumn"
    <div class="content nomargin">
      <%= render :partial => 'admin/admin/nav_edit_header_bar', :locals => {:back_path => @back_path, :show_path => @new_path} %>
      <%= render 'form' %>
      <%= render :partial => 'admin/admin/nav_edit_header_bar', :locals => {:back_path => @back_path, :show_path => @new_path} %>
    </div>
  </div>
</div>
