<%= form_for(@admin_appellation) do |f| %>
  <% if @admin_appellation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_appellation.errors.count, "error") %> prohibited this admin_appellation from being saved:</h2>

      <ul>
      <% @admin_appellation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
