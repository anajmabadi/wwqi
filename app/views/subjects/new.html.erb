<% @page_description = t(:new_description) -%>
<% @page_title = t(:subject).pluralize.capitalize + ' | ' + t(:new).capitalize -%>

<div id="staticContent" class="clearfix">
  <h1><%= @page_title %></h1>
  <%= render 'form' %>
  <%= link_to 'Back', subjects_path %>
</div>
